const VALUES = {
  user: 'x',
  bot: 'o',
  winUser: 'xxx',
  winBot: 'ooo',
  winMes: 'You win!',
  loseMes: 'You lose :/',
  tryAgain: 'Try arain',
};

const CHECK = {
  check: (value) => typeof result === 'object' && result[0] === value,
};
